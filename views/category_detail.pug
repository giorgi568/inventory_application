extends layout

block content
  h1= categoryName
  p= categoryDescription
  each item in items
    li
      a(href=item.url)= item.name 

  a(href=`${categoryUrl}/update`) Update Category
  br
  a(href=`${categoryUrl}/delete`) Delete Category 