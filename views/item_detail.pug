extends layout

block content
  h1= name
  p= description
  em Price: #{price}$
  p Number in Stock #{numberInStock}
  p Category: #{categoryName} 

  br
  br

  a(href=`${itemUrl}/update`) Update Item
  br
  a(href=`${itemUrl}/delete`) Delete Item 